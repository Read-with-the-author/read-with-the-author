<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ site_title }} - {{ bookSummary.title }}{% block title %}{% endblock %}</title>
    <link rel="stylesheet" href="/css/bootstrap-4.4.1-min.css">
</head>
<body>
<div class="container">
    <div class="alerts mb-3 mt-3">
        {% for key, messages in app.flashes() %}
            {% for message in messages %}
                <div class="alert alert-{{ key }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    </div>
    {% block body %}{% endblock %}
</div>
</body>
</html>
